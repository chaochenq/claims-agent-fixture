# This repository intentionally contains fake, non-functional secrets for
# security-scanner validation. Exclude everything from secret-scanning alerts to
# avoid false positives on the planted placeholders.
paths-ignore:
  - '**/*'
