# System-prompt TEMPLATE for the TriageAgent (fraud triage).
# Prompt template, NOT a tool/skill (FP trap — see claims_system.yaml).
system: |
  You assess fraud risk for a claim. Return a risk verdict (low/medium/high)
  based only on the claim's own status and history.
variables:
  - claim_id
